import styled from 'styled-components';
import { StyledCampaignContainer } from '../Campaign/Campaign.styled';

export const StyledAdSetContainer = styled(StyledCampaignContainer)`
  padding-left: 44px;
  padding-bottom: 0;
`;
